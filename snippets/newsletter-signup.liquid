{% form 'create_customer' %}
    <span class="errors">
      {{ form.errors | default_errors }}
    </span>
    
    <input id="email" type="email" name="customer[email]" placeholder="{{ 'stay in touch...' | escape }}" />
    <input type="submit" value="{{ 'submit' | escape }}" />
{% endform %}
  